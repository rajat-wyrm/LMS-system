{
  "name": "lms-root",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:admin\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd course-compass-main && npm run dev",
    "dev:admin": "cd admin && npm run dev",
    "install:all": "npm install --legacy-peer-deps && cd backend && npm install --legacy-peer-deps && cd ../course-compass-main && npm install --legacy-peer-deps && cd ../admin && npm install --legacy-peer-deps"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

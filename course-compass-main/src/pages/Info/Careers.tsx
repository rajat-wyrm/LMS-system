import InfoPlaceholder from "@/pages/Info/InfoPlaceholder";

const Careers = () => {
  return (
    <InfoPlaceholder
      title="Careers"
      description="We are preparing a careers page to share open roles and what it is like to build learning experiences with our team."
    />
  );
};

export default Careers;

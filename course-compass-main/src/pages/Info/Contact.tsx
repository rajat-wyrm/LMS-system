import InfoPlaceholder from "@/pages/Info/InfoPlaceholder";

const Contact = () => {
  return (
    <InfoPlaceholder
      title="Contact"
      description="Our contact page is being set up. For now, this placeholder keeps the footer navigation working while the full screen is prepared."
    />
  );
};

export default Contact;

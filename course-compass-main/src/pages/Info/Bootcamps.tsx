import InfoPlaceholder from "@/pages/Info/InfoPlaceholder";

const Bootcamps = () => {
  return (
    <InfoPlaceholder
      title="Bootcamps"
      description="Intensive cohort-based bootcamps are on the way. Check back soon for immersive programs with guided projects and mentorship."
    />
  );
};

export default Bootcamps;
